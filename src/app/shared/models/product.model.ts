export interface Product {
  code: string;
  group: string;
  title: string;
  // order: number;
  // key: string;
  // img: string;
  // submetric: string;
  // names: string;
  // brand: string;
  // metric: string;
  // status: string;
  price: number;
  qty: number;
}
